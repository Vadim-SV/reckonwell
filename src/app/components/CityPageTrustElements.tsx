import React, { useEffect } from 'react';

const CityTrustBadges = () => {
  React.useEffect(() => {
    // eslint-disable-next-line no-console
    console.warn('Placeholder: CityTrustBadges is not implemented yet.');
  }, []);
  return (
    <>
  { /*CityTrustBadges */} 
 </>
  );
};

export { CityTrustBadges };

const CityFounderBlock: React.FC = () => {
  React.useEffect(() => {
    // eslint-disable-next-line no-console
    console.warn('Placeholder: CityFounderBlock is not implemented yet.');
  }, []);
  return (
    <div>
      {/* CityFounderBlock placeholder */}
    </div>
  );
};

export { CityFounderBlock };